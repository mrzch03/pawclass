/** Default course ID — should be fetched from API in the future */
export const DEFAULT_COURSE_ID = "middle/grade7-up/english";

/** API base URL — empty string means same origin */
export const API_BASE = "";
